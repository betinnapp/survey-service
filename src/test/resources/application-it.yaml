#Server
server:
  port: 0

management:
  port: 0

#Datasource config
spring:
  datasource:
    hikari:
      jdbc-url: jdbc:postgresql://172.17.0.1:41611/postgres
      username: survey-service-app
      password: survey-service-app
      driverClassName: org.postgresql.Driver
